# Optional - the leaderboard and analytics run fully local without these
# (see src/lib/supabaseClient.ts). Nothing is required for local dev.
#
# Once you create a Supabase project, run supabase/schema.sql in its SQL
# editor, then fill these in to make the leaderboard shared/global.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
